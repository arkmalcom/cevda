[project]
name = "cevda-backend"
version = "0.1.0"
description = "Backend scripts for CEVDA project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.9.1",
]
